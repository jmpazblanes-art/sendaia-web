# Sesión 2026-08-19

## Proyecto
sendaia-web

## ✅ Qué se hizo
- **Limpieza de ramas huérfanas**:
  - Eliminadas ramas locales y remotas obsoletas (`feat/gsap-premium`, `feat/hero-3d`, `feat/logo-3d`, `feat/tracking-web`, `codex/sendaia-web-story-ads`, `fix/auditoria-a`).
  - Única rama activa: `main`.
- **Eliminación de endpoint muerto**:
  - Borrado `app/api/chat/route.ts` (versión antigua de OpenAI `gpt-4o-mini`). Aria ahora utiliza centralizadamente el backend de `whatsapp-sendaia.vercel.app/api/chat-web`.
- **Integración de enlaces y schema**:
  - Aplicado enlace a Facebook en el footer de la web y en el grafo de `sameAs` del schema JSON-LD en `app/layout.tsx`.
  - Añadido Facebook al prompt comercial de Aria.
- **Optimización y auditoría Mobile**:
  - Añadido `scroll-padding-top: 5.5rem` en `app/globals.css` para que la barra de navegación fija no tape los títulos de sección al hacer clic en los enlaces del `MenuMovil`.
  - Corregido tamaño de fuente en campos de entrada (`text-base sm:text-sm`) para evitar el auto-zoom involuntario de iOS Safari al enfocar inputs.
  - Ajustado ancho y márgenes simétricos del modal de Aria en pantallas estrechas (`w-[calc(100vw-2rem)] right-4 sm:right-6`).
- **4 Disparadores de Conversión y Captación (CRO & SEO)**:
  1. **Calculadora Interactiva de ROI (`CalculadoraROI`)**: Sliders dinámicos de horas (5-40h), personas (1-10) y coste/hora (12-50€) que calculan en directo horas ahorradas al mes y ahorro económico anual con botón CTA contextual. Añadido enlace a la Calculadora en la cabecera de escritorio y en el menú móvil.
  2. **Micro-bocadillo gancho en Aria**: Tooltip sutil tras 4.5 segundos (*«¿Qué proceso te roba más tiempo? Cuéntamelo aquí y te digo cómo automatizarlo»*) que invita a chatear.
  3. **Canal dual de WhatsApp**: Opción directa en formulario de contacto y botón de WhatsApp contextual en las 6 páginas de sector (`/sectores/...`) con pre-rellenado específico.
  4. **Schema SEO `FAQPage`**: Microdatos estructurados en `app/layout.tsx` respondiendo a preguntas frecuentes comerciales para snippets de Google y motores de IA (ChatGPT, Perplexity).

## 📈 Avances verificados
- Pruebas en navegador real (`browser_subagent`) validando la calculadora interactiva, el bocadillo flotante de Aria y el formulario con WhatsApp.
- Verificado con `npm run build` (17 rutas SSG/Static generadas sin errores).
- Verificado con pre-push hook (`tipos ✅`) y subido a `origin/main`.

## 🎯 Decisiones técnicas
- Mantener diseño visual prémium sin alterar la paleta de colores ni tipografía Fraunces/Manrope.
- Mantener cambios 100% atómicos y reversibles en Git.
- Respetar regla dura de `CLAUDE.md`: todo el trabajo directamente en `main` desplegado el mismo día.

## 📝 Commits de hoy
```
c4c7c5d feat(cro): calculadora ROI interactiva, gancho flotante Aria, WhatsApp dual y schema FAQPage
c4661f3 fix(mobile): scroll-padding para menu movil, inputs sin auto-zoom iOS y ajuste de modal Aria
42016cf docs: actualizar pendientes tras limpieza completa de ramas
d5ea0a4 feat(seo): enlace Facebook en footer y schema sameAs + borrado api/chat obsoleto
```
