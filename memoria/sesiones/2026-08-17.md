# Sesión 2026-08-17

## Proyecto
sendaia-web

## ✅ Qué se hizo
- Diagnosticado por qué sendaia.es mostraba una versión de la web sin WhatsApp,
  sin Aria, sin agente de voz, sin vídeos optimizados: `main` en GitHub llevaba
  desde el 22-may (commit `42ffe096`, "Use real social links") sin actualizarse,
  mientras TODO el trabajo real de estos 3 meses vivía en `fix/auditoria-a` y se
  desplegaba directamente desde ahí.
- El 11-ago (6 días antes de esta sesión) algo volvió a desplegar `main` vieja
  como producción, pisando en silencio el deploy bueno del 3-ago. HTTP 200 todo
  el rato — por eso nadie lo notó hasta que Pachi abrió la web hoy.
- Arreglo inmediato: `vercel alias set` al deploy bueno del 3-ago mientras se
  investigaba a fondo.
- Arreglo de raíz: `git merge origin/fix/auditoria-a` → `main` (fast-forward,
  sin conflictos) + push. Vercel redesplegó automáticamente y promovió ese
  deploy a producción.
- Verificado en navegador real (no solo HTTP 200): WhatsApp, Aria, agente de
  voz, vídeo del hero, red neuronal de fondo — todo presente, con capturas.
- Medido el coste real de los 6 días rotos: consulta a `site_events` en
  Supabase (proyecto `rudolmemxsugxmcbvrwe`) → 6 visitas en total del 11 al 17
  de agosto, sin pico anómalo. No hay evidencia de que un lead grande o Ana
  entraran justo en la ventana rota.
- Creado `CLAUDE.md` en la raíz del repo con la regla dura: nunca dejar una
  rama con deploys de producción propios viviendo días/semanas sin fusionar a
  `main` el mismo día que se despliega.
- Montado un guardarraíl automático: GitHub Action
  `.github/workflows/check-produccion.yml` que compara el HTML real de
  `sendaia.es` contra 3 marcadores clave (WhatsApp, Agente de voz, Aria) en
  cada push a `main` y una vez al día por cron (08:13 UTC). Si falta alguno,
  avisa por Telegram (bot OPS ya existente del CRM, mismo canal que los leads
  calientes) y marca el job en rojo en GitHub.
- El primer intento del workflow tenía un marcador mal elegido
  (`"WhatsAppButton"`, el nombre del componente React, que nunca aparece en
  el HTML renderizado) → falso positivo, mandó una alerta real de Telegram sin
  motivo. Corregido: solo se comprueban strings que sí están en el HTML final.
- Guardado el incidente completo en el cerebro central:
  `bug_sendaia_web_main_desincronizada_pisaba_produccion.md`, enlazado desde
  `proyecto_sendaia_web` y desde el índice `MEMORY.md`.

## 🐛 Bugs encontrados
- `main` desincronizada 3 meses de la rama de trabajo real, pisó producción
  el 11-ago sin que nadie se enterara (HTTP 200 no distingue código viejo de
  código bueno).
- Falso positivo en el primer workflow de vigilancia (marcador
  "WhatsAppButton" no existe en HTML renderizado) — corregido en la misma
  sesión.

## ✅ Bugs solucionados
- Web en producción restaurada a la versión completa (WhatsApp + Aria + voz +
  vídeos + legal RGPD + SEO). Verificado visualmente.
- `main` y `fix/auditoria-a` fusionadas — ya no hay dos versiones de la verdad.
- Falso positivo del check de vigilancia corregido y verificado contra el
  HTML real antes de subir.

## 📈 Avances verificados
- sendaia.es sirve el código correcto: confirmado con navegador real y con
  `document.querySelector` sobre el DOM en vivo.
- GitHub Action de vigilancia diaria activo y probado end-to-end (incluido el
  envío real de un mensaje de Telegram cuando detectó el falso positivo, lo
  que confirma que el canal de aviso funciona).

## 🚀 Deploys
- `dpl_6EDtNeSNyBoW3H4iXGBD5r2wfyQk` — commit `200e687` (merge de
  fix/auditoria-a a main) — producción, verificado.
- Deploys posteriores automáticos por los 2 commits de fix del workflow
  (`98ba2ff`, `00b4c6d`) — no afectan al contenido de la web, solo a
  `.github/workflows/` y `CLAUDE.md`.

## 🎯 Decisiones técnicas
- A partir de ahora, todo el trabajo de sendaia-web se hace en `main` o se
  fusiona a `main` el mismo día que se despliega a producción. Nunca dejar
  una rama de trabajo con deploys de producción propios viviendo semanas sin
  fusionar (regla en `CLAUDE.md` del repo).
- Antes de dar la web por buena, verificar SIEMPRE con navegador real
  (WhatsApp, Aria, agente de voz) — un HTTP 200 no certifica que el código
  sea el correcto.
- Guardarraíl automático preferido sobre depender de que alguien recuerde
  comprobarlo a mano.

## 📝 Commits
```
00b4c6d fix(ops): quitar marcador WhatsAppButton del check — falso positivo
98ba2ff fix(ops): simplificar el workflow de vigilancia, el heredoc rompía el parseo
745c8e3 ops: vigilar que sendaia.es sirve el código de main, avisar por Telegram si diverge
200e687 legal: datos bancarios y Enable Banking en la politica de privacidad (merge fix/auditoria-a)
```

## ⏱️ Horas
Pendientes de registrar — sesión larga (diagnóstico + arreglo + guardarraíl),
sin cronometrar en vivo. Registrar en `/horas` en la próxima sesión.

## ➡️ Próximo paso exacto
Ninguno bloqueante en sendaia-web. El guardarraíl (run `32029351689`) terminó
en verde (`success`) antes de cerrar la sesión — confirmado. Queda solo
decidir el cambio en `git stash` (link de Facebook) cuando Pachi lo confirme.

## 🚫 Qué NO tocar mañana
- No volver a trabajar en una rama distinta de `main` para sendaia-web sin
  fusionar el mismo día.
- No tocar `.github/workflows/check-produccion.yml` sin probar el marcador a
  mano contra `curl -sL https://sendaia.es/` antes de subir (así se detectó
  el falso positivo de hoy).

## ⚠️ Pendiente de clasificar
- Cambio sin commitear (enlace a Facebook en el footer + schema SEO) guardado
  en `git stash` (`stash@{0}`, mensaje "footer+schema: link Facebook
  pendiente de confirmar con Pachi") — a la espera de que Pachi confirme si
  lo quiere antes de aplicarlo.
- No se pudo determinar CON CERTEZA quién/qué disparó el deploy accidental de
  `main` el 11-ago (no hay logs de auditoría de Vercel accesibles desde aquí
  para esa fecha). Si se quiere cerrar esa duda, revisar el dashboard de
  Vercel → Deployments → historial de actividad del 11-ago manualmente.
